# Story 13.3 — Migration de limpeza e validação end-to-end

## Metadata

```yaml
story_id: 13.3
epic_id: EPIC-013
title: Migration de limpeza de registros órfãos e validação completa
status: Ready
executor: "@dev"
quality_gate: "@qa"
created_by: "@pm (Morgan)"
created_at: 2026-04-14
complexity: S (2 pontos)
risk: Low
```

---

## Descrição

Como **coordenador**, quero que os dados de equipe no banco estejam consistentes — sem registros duplicados, gaps ou órfãos — e que todas as 4 turmas ativas (Cohort Advanced T2, Cohort Fundamentals T4, PS Advanced, PS Fundamentals) mostrem equipe correta no calendário e no card.

---

## Acceptance Criteria

- [ ] **AC1 — Limpar duplicatas:** Migration identifica e remove registros duplicados em `class_mentors` (mesmo class_id + mentor_id + role + weekday + valid_from)
- [ ] **AC2 — Validar turmas ativas:** Query de validação confirma que todas as 4 turmas ativas têm pelo menos 1 membro com `valid_until IS NULL` em cada weekday configurado
- [ ] **AC3 — Calendário correto:** Para cada turma ativa, verificar que `buildEventsFromDB` retorna mentores para datas passadas (histórico) e futuras (equipe atual)
- [ ] **AC4 — Sem registros órfãos:** Nenhum registro em `class_mentors` referencia `class_id` ou `mentor_id` inexistente
- [ ] **AC5 — Migration idempotente:** A migration pode ser executada múltiplas vezes sem efeito colateral

---

## Scope

### IN
- Migration SQL de limpeza
- Script de validação (query que verifica consistência)
- Correção de dados inconsistentes encontrados

### OUT
- Alterações no código JS — já feitas nas Stories 13.1 e 13.2
- Schema changes — nenhuma alteração de schema

---

## Tasks

- [ ] Criar migration `20260414050000_cleanup_class_mentors.sql`
- [ ] Identificar e remover duplicatas exatas
- [ ] Identificar registros com class_id/mentor_id inexistentes e remover
- [ ] Executar query de validação nas 4 turmas ativas
- [ ] Verificar calendário renderiza corretamente para datas de transição
- [ ] Aplicar migration em produção

---

## Validation Query

```sql
-- Verifica que toda turma ativa tem equipe ativa
SELECT c.name, cm.weekday, COUNT(*) as active_members
FROM classes c
JOIN class_mentors cm ON cm.class_id = c.id
WHERE c.active = true AND cm.valid_until IS NULL
GROUP BY c.name, cm.weekday
ORDER BY c.name, cm.weekday;
```

---

## File List

- `supabase/migrations/20260414050000_cleanup_class_mentors.sql`
