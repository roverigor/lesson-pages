# Story 22.1 — Unificar identidade do aluno (phone E.164 + VIEW canônica)

## Metadata

```yaml
story_id: 22.1
epic_id: EPIC-022
title: Unificar identidade do aluno — normalizar phone E.164 + trigger + VIEW v_students_unified + UNIQUE constraint
status: Draft
executor: "@data-engineer"
quality_gate: "@qa"
created_by: "@aiox-master (Orion)"
created_at: 2026-05-22
complexity: L  # ~3-5 dias com backfill prod + validação smoke + dry-run
risk: High  # backfill em prod com 1k+ alunos + UNIQUE constraint pode falhar se duplicates + trigger add latency
priority: P0  # JOINs falham silenciosamente, alunos perdidos em campanhas (impact: business)
references:
  - docs/epics/EPIC-022-painel-refactor.md (S.022.1 spec)
  - docs/discovery/2026-05-22/02-db-audit.md §3.1 (triple-identity), §3.2 (phone format), §4 (paths múltiplos)
  - supabase/migrations/20260409050000_normalize_student_phones.sql (RPC existente, sem trigger)
  - .claude/CLAUDE.md (Comunicação Externa — gate humano pra apply prod)
```

## Status
Draft

## Epic
[EPIC-022 — Painel Refactor: Consolidação Backend + Foundation](../epics/EPIC-022-painel-refactor.md)

## Priority
P0 — JOINs falhando silenciosamente em campanhas (alunos perdidos quando phone format diverge entre tabelas)

## Story
Como **administrador da Academia Lendária**,
Eu preciso que **todas as 3 tabelas que representam aluno (`students`, `student_imports`, `wa_group_members`) tenham coluna `normalized_phone` em formato E.164 (+55DDD9XXXXXXXX) garantida por trigger + VIEW canônica `v_students_unified` que mescle as 3 fontes**,
Para que **campanhas (NPS, lembretes, PS RSVP) usem 1 identidade única por aluno e nenhum aluno seja perdido por phone format divergente**.

## Contexto

Discovery `02-db-audit.md` §3.1 identificou **triple-identity** crítico:

| Tabela | Origem | Phone key |
|--------|--------|-----------|
| `students` | CSV manual + admin entries | UNIQUE(phone, cohort_id) — formato heterogêneo |
| `student_imports` | AC webhook payload JSONB | Sem constraint — pode ter "+55", "0x", espaços |
| `wa_group_members` | Sync cron WA group | UNIQUE(cohort_id, phone) — format WA API |

**Problema raiz:** phone formats divergem (`+5511987654321` vs `11987654321` vs `5511987654321`). JOINs por phone falham silenciosamente. `dispatch_link_opens` + `nps_class_links` não sabem qual `student_id` usar. Campanhas perdem alunos (impacto direto: receita NPS + retenção).

**Estado atual:**
- Migração `20260409050000_normalize_student_phones.sql` definiu `normalize_student_phones()` RPC — mas SEM trigger automático
- Backfill incompleto: novos alunos via AC ou WA podem entrar com phone não-normalizado
- Não existe VIEW canônica — clientes (edge functions, RPCs) precisam JOIN manual com lógica frágil

**Decisão arquitetural confirmada:**
- `students` = **source-of-truth** (admin canonical)
- `student_imports` = **audit log AC** (nunca deletar — auditoria de compras)
- `wa_group_members` = **espelho WA** (sync automático)
- VIEW `v_students_unified` resolve identidade por `(normalized_phone, cohort_id)`

**Sistema EM PRODUÇÃO** com 1k+ alunos ativos. Backfill exige idempotência + dry-run + rollback + smoke + Slack alert + janela manutenção (low-traffic).

---

## Acceptance Criteria

### AC1 — Coluna `normalized_phone` em 3 tabelas
**Given** schema atual (sem coluna normalizada explícita)
**When** migration aplica
**Then** `students`, `student_imports`, `wa_group_members` têm coluna `normalized_phone text` (nullable durante backfill, NOT NULL após T6)

### AC2 — Função `normalize_phone_e164(text) returns text`
**Given** input phone em qualquer format ("+5511987654321", "11 98765-4321", "5511987654321", "+55 (11) 98765-4321")
**When** chama `normalize_phone_e164(input)`
**Then** retorna E.164 `+55DDD9XXXXXXXX` (13 dígitos pós-`+`)
**E** retorna NULL se input inválido (< 10 dígitos ou > 14 dígitos após strip)

### AC3 — Trigger BEFORE INSERT/UPDATE
**Given** trigger criado nas 3 tabelas
**When** INSERT ou UPDATE com `phone` setado
**Then** `normalized_phone` é populado automaticamente via `normalize_phone_e164(NEW.phone)`
**E** trigger não falha se `phone` é NULL (deixa `normalized_phone` NULL também)

### AC4 — Backfill idempotente
**Given** dados existentes nas 3 tabelas
**When** roda RPC `backfill_normalized_phones()` 1x ou 2x
**Then** rows com `normalized_phone IS NULL` recebem valor populado
**E** rows com `normalized_phone` já populado NÃO são modificadas (idempotente)
**E** log estruturado mostra count atualizado por tabela

### AC5 — VIEW `v_students_unified`
**Given** dados normalizados nas 3 tabelas
**When** query `SELECT * FROM v_students_unified WHERE cohort_id = 'T5'`
**Then** retorna 1 linha por `(normalized_phone, cohort_id)` mesclando:
- `students` columns (canonical id, name, is_mentor, active)
- `student_imports` columns (product, source, ac_event_id)
- `wa_group_members` columns (wa_name, group_id, synced_at)
**E** colunas conflitantes (ex: `name`) priorizam `students.name` > `wa_group_members.wa_name` > `student_imports.name`

### AC6 — UNIQUE(normalized_phone, cohort_id) em `students`
**Given** backfill completo + sem duplicates
**When** migration adiciona constraint
**Then** `ALTER TABLE students ADD CONSTRAINT students_normalized_phone_cohort_unique UNIQUE (normalized_phone, cohort_id)` passa
**E** rodar `find_duplicate_students()` RPC retorna 0 duplicates

### AC7 — Smoke test (10 cenários)
- [ ] T1: input "+5511987654321" → output "+5511987654321"
- [ ] T2: input "11987654321" → output "+5511987654321"
- [ ] T3: input "5511987654321" → output "+5511987654321"
- [ ] T4: input "(11) 98765-4321" → output "+5511987654321"
- [ ] T5: input " 11 98765 4321 " (whitespace) → output "+5511987654321"
- [ ] T6: input "11 98765-4321 " → output "+5511987654321"
- [ ] T7: input NULL → output NULL (não falha)
- [ ] T8: input "" → output NULL
- [ ] T9: input "abc123" (inválido) → output NULL
- [ ] T10: backfill rodado 2x → diff zero

### AC8 — Slack alert pre/post apply
**Given** wrapper `apply-with-slack-alert.sh` (reusado de 22.4)
**When** migration aplica
**Then** Slack #devops recebe:
- Pre: tabelas escopo + operador + ts + dry_run flag
- Post: status + duração + count rows backfilled + output trunc

### AC9 — Backwards-compat
**Given** queries legacy usam `phone` (não-normalizado)
**When** migration aplica
**Then** coluna `phone` original é PRESERVADA por 30 dias (não dropada)
**E** queries legacy continuam OK
**E** ADR-021 documenta deprecation timeline

### AC10 — Audit trail
**Given** `audit_log` recebe insert no início + fim da migration
**When** migration completa
**Then** event_type='identity_unification' com payload contendo:
- story_id, migration name, started_at/completed_at, tables_affected, rows_backfilled

### AC11 — Validation SQL pós-apply
```sql
-- Todas rows em students têm normalized_phone E.164 válido
SELECT count(*) FROM students
WHERE normalized_phone IS NULL
   OR normalized_phone !~ '^\+55[1-9][1-9][0-9]{8,9}$';
-- expected: 0
```

---

## Scope

### IN scope
- Função `normalize_phone_e164(text) returns text`
- Coluna `normalized_phone` em `students`, `student_imports`, `wa_group_members`
- Trigger BEFORE INSERT/UPDATE nas 3 tabelas
- RPC `backfill_normalized_phones()` idempotente
- VIEW `v_students_unified`
- UNIQUE constraint `(normalized_phone, cohort_id)` em `students`
- Smoke script bash + 10 cenários
- ADR-021 documentando decisão
- Migration up + down pareada
- Wrapper Slack reusando 22.4 pattern

### OUT of scope (story separada futura)
- Drop coluna `phone` legacy (postergado 30 dias — outra story)
- Refactor edge functions pra usar `v_students_unified` (gradual — outras stories)
- Merge automático cross-table (continua manual via `merge_students()` RPC)
- Aplicar UNIQUE em `student_imports` e `wa_group_members` (mantém open append-only)
- Reconciliação automatic com Hotmart/AC quando phone muda

---

## Tasks/Subtasks

- [ ] **T1 — Validar estado atual phones em prod**
  - [ ] Query `SELECT count(*), substring(phone from 1 for 4) as prefix FROM students GROUP BY 2 ORDER BY 1 DESC` — heatmap formats
  - [ ] Query `SELECT count(*) FROM student_imports WHERE phone IS NULL OR phone = ''` — invalid rows
  - [ ] Query `SELECT count(*) FROM wa_group_members WHERE phone !~ '^\\+?[0-9]+$'` — formats inesperados
  - [ ] Documentar diff em `qa/22.1-prod-recount.md`
  - [ ] Estimar % rows que vão falhar normalização (NULL após backfill) — gate <5%

- [ ] **T2 — Criar função `normalize_phone_e164(text) returns text`**
  - [ ] STABLE + IMMUTABLE
  - [ ] Strip whitespace, parênteses, hifens, espaços
  - [ ] Se input não-numérico após strip → NULL
  - [ ] Se 10-11 dígitos (BR sem 55) → prepend "+55"
  - [ ] Se 12-13 dígitos (BR com 55 sem +) → prepend "+"
  - [ ] Se 13 dígitos com "+" → return as-is
  - [ ] Validar pattern final `^\+55[1-9][1-9][0-9]{8,9}$`
  - [ ] Se não bater pattern → NULL
  - [ ] Unit tests inline via DO block (10 cenários AC7)

- [ ] **T3 — Adicionar coluna `normalized_phone` em 3 tabelas**
  - [ ] `ALTER TABLE students ADD COLUMN normalized_phone text`
  - [ ] `ALTER TABLE student_imports ADD COLUMN normalized_phone text`
  - [ ] `ALTER TABLE wa_group_members ADD COLUMN normalized_phone text`
  - [ ] Coluna nullable durante backfill (NOT NULL só após T6 em `students`)

- [ ] **T4 — Trigger BEFORE INSERT/UPDATE**
  - [ ] `CREATE OR REPLACE FUNCTION trigger_normalize_phone() RETURNS trigger LANGUAGE plpgsql`
  - [ ] Body: `NEW.normalized_phone := public.normalize_phone_e164(NEW.phone); RETURN NEW;`
  - [ ] `CREATE TRIGGER ... BEFORE INSERT OR UPDATE OF phone ON students FOR EACH ROW EXECUTE FUNCTION trigger_normalize_phone()`
  - [ ] Idem `student_imports`, `wa_group_members`
  - [ ] DROP IF EXISTS antes de cada CREATE (idempotência)

- [ ] **T5 — RPC `backfill_normalized_phones()`**
  - [ ] `CREATE OR REPLACE FUNCTION backfill_normalized_phones() RETURNS TABLE(table_name text, rows_updated int)`
  - [ ] UPDATE WHERE normalized_phone IS NULL em cada uma das 3 tabelas
  - [ ] RETURN counts por tabela
  - [ ] Idempotente: 2x → diff zero (segundo run retorna 0 em todas)

- [ ] **T6 — VIEW `v_students_unified`**
  - [ ] LEFT JOIN students s ⟕ student_imports si ON (s.normalized_phone = si.normalized_phone AND s.cohort_id = si.cohort_id)
  - [ ] LEFT JOIN wa_group_members wgm ON (s.normalized_phone = wgm.normalized_phone AND s.cohort_id = wgm.cohort_id)
  - [ ] Colunas: COALESCE(s.name, wgm.wa_name, si.name) AS canonical_name + as columns each source
  - [ ] WHERE s.normalized_phone IS NOT NULL (descarta lixo)
  - [ ] Documentar dependency direta no ADR-021

- [ ] **T7 — UNIQUE constraint em `students`**
  - [ ] Rodar `find_duplicate_students()` RPC pré-apply
  - [ ] Se duplicates > 0 → BLOCK + alert admin pra merge manual ANTES
  - [ ] `ALTER TABLE students ADD CONSTRAINT students_normalized_phone_cohort_unique UNIQUE (normalized_phone, cohort_id)`
  - [ ] `ALTER TABLE students ALTER COLUMN normalized_phone SET NOT NULL` (após backfill OK)

- [ ] **T8 — Migration down (rollback)**
  - [ ] Arquivo `_unified_identity.down.sql`
  - [ ] DROP CONSTRAINT students_normalized_phone_cohort_unique
  - [ ] DROP VIEW v_students_unified
  - [ ] DROP TRIGGER em 3 tabelas
  - [ ] DROP FUNCTION trigger_normalize_phone, backfill_normalized_phones, normalize_phone_e164
  - [ ] ALTER TABLE students/student_imports/wa_group_members DROP COLUMN normalized_phone (com IF EXISTS pra idempotência)
  - [ ] PRESERVA coluna `phone` original (out-of-scope drop)

- [ ] **T9 — Smoke script**
  - [ ] `scripts/smoke-identity-test.sh` cobrindo AC7 (10 cenários + RPC backfill check)
  - [ ] Pattern reusado de `scripts/smoke-rls-test.sh`
  - [ ] Exit code + log `qa/22.1-smoke-{ts}.log`

- [ ] **T10 — ADR-021**
  - [ ] `docs/architecture/ADR-021-student-identity-unification.md`
  - [ ] Decisão: E.164 source-of-truth + students canonical + audit/mirror
  - [ ] Alternativas (UUID identity service, dedupe automático) — rejeitadas com razão
  - [ ] Deprecation timeline coluna `phone` legacy (30 dias)
  - [ ] Dependências futuras: edge functions migration → outras stories

- [ ] **T11 — Dry-run PG 15 local**
  - [ ] pg_dump schema + sample data prod
  - [ ] Apply migration up → smoke → apply down → smoke → reapply up
  - [ ] Validar backfill rodando em ~1k rows não trava (perf < 5s)
  - [ ] Log `qa/22.1-dry-run-{ts}.log`

- [ ] **T12 — Apply prod (GATE HUMANO)**
  - [ ] **NON-NEGOTIABLE:** aprovação literal user por mensagem antes apply
  - [ ] Slack pre-alert
  - [ ] Janela manutenção (madrugada — low traffic)
  - [ ] `supabase db push` via wrapper
  - [ ] Slack post-alert
  - [ ] Smoke prod completo
  - [ ] AC11 validation query (count = 0)

---

## Dev Notes

### Stack & ferramentas
- **Supabase project:** `gpufcipkajppykmnmdeh` (calendario-aulas)
- **Migrations dir:** `supabase/migrations/`
- **Pattern reference:** `20260522155830_epic_022_s04_rls_hardening.sql` (idempotência + audit + BEGIN/COMMIT por section)
- **Helper existente:** `normalize_student_phones()` em `20260409050000` — vamos refatorar pra função pura sem side-effect (atual UPDATE em tabela)

### Templates SQL

#### Função normalize_phone_e164
```sql
CREATE OR REPLACE FUNCTION public.normalize_phone_e164(input text)
RETURNS text
LANGUAGE plpgsql
IMMUTABLE
AS $$
DECLARE
  cleaned text;
  digits_only text;
BEGIN
  IF input IS NULL OR length(trim(input)) = 0 THEN
    RETURN NULL;
  END IF;

  -- Strip everything except digits and leading +
  cleaned := regexp_replace(input, '[^\d+]', '', 'g');
  digits_only := regexp_replace(cleaned, '^\+', '', 'g');

  -- Length-based normalization
  IF length(digits_only) < 10 OR length(digits_only) > 13 THEN
    RETURN NULL;
  END IF;

  -- Already starts with 55 (BR country code)?
  IF substring(digits_only, 1, 2) = '55' AND length(digits_only) IN (12, 13) THEN
    cleaned := '+' || digits_only;
  ELSIF length(digits_only) IN (10, 11) THEN
    cleaned := '+55' || digits_only;
  ELSE
    RETURN NULL;
  END IF;

  -- Final pattern validation E.164 BR
  IF cleaned ~ '^\+55[1-9][1-9][0-9]{8,9}$' THEN
    RETURN cleaned;
  ELSE
    RETURN NULL;
  END IF;
END;
$$;
```

#### Trigger
```sql
CREATE OR REPLACE FUNCTION public.trigger_normalize_phone()
RETURNS trigger
LANGUAGE plpgsql
AS $$
BEGIN
  NEW.normalized_phone := public.normalize_phone_e164(NEW.phone);
  RETURN NEW;
END;
$$;

DROP TRIGGER IF EXISTS trg_students_normalize_phone ON public.students;
CREATE TRIGGER trg_students_normalize_phone
  BEFORE INSERT OR UPDATE OF phone
  ON public.students
  FOR EACH ROW
  EXECUTE FUNCTION public.trigger_normalize_phone();
-- Repetir pra student_imports + wa_group_members
```

#### VIEW canônica
```sql
CREATE OR REPLACE VIEW public.v_students_unified AS
SELECT
  s.id                                                AS student_id,
  s.normalized_phone,
  s.cohort_id,
  COALESCE(s.name, wgm.wa_name, si.name)              AS canonical_name,
  s.is_mentor,
  s.active,

  -- AC import context
  si.product                                          AS ac_product,
  si.source                                           AS ac_source,
  si.created_at                                       AS ac_imported_at,

  -- WA group context
  wgm.group_id                                        AS wa_group_id,
  wgm.synced_at                                       AS wa_synced_at,

  -- Raw originals (debug/audit)
  s.phone                                             AS student_phone_raw,
  si.phone                                            AS import_phone_raw,
  wgm.phone                                           AS wa_phone_raw
FROM public.students s
LEFT JOIN public.student_imports si
  ON s.normalized_phone = si.normalized_phone
  AND s.cohort_id = si.cohort_id
LEFT JOIN public.wa_group_members wgm
  ON s.normalized_phone = wgm.normalized_phone
  AND s.cohort_id = wgm.cohort_id
WHERE s.normalized_phone IS NOT NULL;

COMMENT ON VIEW public.v_students_unified IS
  'Canonical view de aluno por (normalized_phone, cohort_id). Source-of-truth: students. Mescla audit (student_imports) + mirror WA (wa_group_members). Ref ADR-021.';
```

---

## Riscos

| ID | Risco | Probabilidade | Impacto | Mitigação |
|----|-------|---------------|---------|-----------|
| R1 | Phones inválidos em prod resultam em normalized_phone NULL | Alta | Médio | T1 audit pré-apply; threshold <5% NULL após backfill |
| R2 | UNIQUE constraint falha se duplicates não-detectados | Média | Alto | T7 gate: `find_duplicate_students()` deve retornar 0 antes constraint |
| R3 | Trigger add latency em INSERT/UPDATE | Baixa | Baixo | Função IMMUTABLE + STABLE — overhead ~µs |
| R4 | VIEW lenta pra cohorts grandes (LEFT JOIN x3) | Média | Médio | Indexar `normalized_phone` nas 3 tabelas (parte do T3) |
| R5 | Queries legacy quebram após backfill | Média | Alto | T8 manter coluna `phone` original 30 dias (deprecation) |
| R6 | Backfill em prod trava (1k+ rows × 3 tabelas) | Baixa | Médio | Dry-run T11; rodar em batches se > 10s |
| R7 | Pattern E.164 BR não cobre phones internacionais | Baixa | Baixo | Documentar em ADR-021; story futura se aparecer aluno EU/US |
| R8 | AC webhook continua escrevendo phone bruto | N/A | Baixo | Trigger normaliza em INSERT — sem mudança no webhook |
| R9 | Edge functions usando `phone` em FROM clause | Média | Médio | T1 audit grep edge functions; documentar dependencies |
| R10 | RLS bloqueia VIEW pra authenticated | Baixa | Baixo | VIEW herda RLS das tabelas base (Tier 1 via 22.4) — só admin |
| R11 | normalize_phone_e164 retorna NULL pra phone válido raro | Baixa | Médio | Unit tests T2 cobrir edge cases (e.g. 8 dígitos legacy DDD 11) |
| R12 | Backfill cobre student_imports JSONB bruto | Média | Baixo | T1 query extrai phone via `(payload->>'phone')::text` se necessário |
| R13 | Janela manutenção coincide com cron WA sync | Média | Médio | T12 desabilitar cron WA durante apply — restart pós-validation |

---

## Dependencies

- **Depends on:** Story 22.4 (RLS Hardening) — VIEW herda RLS das base tables; safer aplicar APÓS 22.4
- **Blocks:** Stories que querem usar `v_students_unified` (futuras 22.2 dispatch refactor, 22.3 dashboards)

---

## Change Log
- 2026-05-22 @aiox-master — Story criada (drafting via @sm methodology); status Draft; aguarda @po validation
