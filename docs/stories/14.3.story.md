# Story 14.3 — Agendar alerta de ausência para alunos no cron

## Metadata

```yaml
story_id: 14.3
epic_id: EPIC-014
title: Agendar send_absence_alerts no pg_cron para notificar alunos com faltas consecutivas
status: Draft
executor: "@data-engineer"
quality_gate: "@qa"
created_by: "@sm (River)"
created_at: 2026-04-16
complexity: S (3 pontos)
risk: Low
```

---

## Descrição

Como **coordenador**, quero que alunos com 2 ou mais faltas consecutivas recebam automaticamente uma mensagem de acompanhamento via WhatsApp — para que eu não precise monitorar faltas manualmente e a retenção de alunos melhore.

### Contexto Técnico

- A action `send_absence_alerts` já existe em `zoom-attendance/index.ts` (linhas ~1111-1180)
- Ela busca alunos com 2+ faltas consecutivas em `student_attendance` e envia WhatsApp
- Porém **não está agendada em nenhum cron job** — só pode ser chamada manualmente
- O horário ideal é após o pipeline diário (que roda às 03:00 BRT) e após o staff reminder (08:00 BRT)

---

## Acceptance Criteria

- [ ] **AC1 — Cron job criado:** `pg_cron` agenda `absence-alerts` diário em horário adequado (sugestão: 09:00 BRT / 12:00 UTC)
- [ ] **AC2 — Trigger function:** Criar `trigger_absence_alerts()` que chama a edge function `zoom-attendance` com `action = 'send_absence_alerts'`
- [ ] **AC3 — Apenas dias de aula:** O alerta só é enviado em dias úteis (seg-sex), não em finais de semana
- [ ] **AC4 — Anti-spam:** Um aluno só recebe 1 alerta por semana, mesmo que tenha faltas em múltiplas turmas
- [ ] **AC5 — Log no automation_runs:** Execução logada com `p_run_type = 'absence_alerts'`
- [ ] **AC6 — Health check integrado:** O health check diário (06:00 BRT) passa a monitorar também `absence_alerts`

---

## Scope

### IN
- Migration com `pg_cron.schedule` para `absence-alerts`
- Trigger function `trigger_absence_alerts()`
- Verificar/ajustar anti-spam na action existente
- Atualizar health check para monitorar novo job

### OUT
- Reescrever `send_absence_alerts` (já funciona, só precisa agendar)
- Alterar template da mensagem (fase futura)
- Dashboard de acompanhamento de faltas (fase futura)

---

## Tasks

- [ ] Ler action `send_absence_alerts` existente e validar lógica
- [ ] Criar migration com trigger function + pg_cron schedule
- [ ] Verificar se anti-spam (1 alerta/semana) está implementado
- [ ] Se não, adicionar guard na action
- [ ] Atualizar health check para incluir `absence_alerts` no monitoramento
- [ ] Testar: cron job aparece em `cron.job`
- [ ] Testar: chamada manual retorna resultado esperado

---

## Dependencies

- `zoom-attendance/index.ts` — action `send_absence_alerts` existente
- Tabela `student_attendance` — presença dos alunos
- Tabela `notifications` — para envio WhatsApp
- Pipeline diário deve ter rodado antes (presença atualizada)

---

## Notas

- **Validar com Igor antes de ativar:** O envio automático de WhatsApp para alunos precisa de autorização explícita antes de ser ligado em produção (regra: nunca dispatch sem autorização)
- O cron pode ser criado desabilitado inicialmente e ativado após aprovação
