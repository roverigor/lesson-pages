# Story 9.1 — Audit Trail de Vinculações Zoom→Aluno

## Metadata

```yaml
story_id: 9.1
epic_id: EPIC-009
title: Audit trail de vinculações Zoom→Aluno
status: Done
executor: "@dev"
quality_gate: "@qa"
created_by: "@sm (River)"
created_at: 2026-04-07
complexity: S (2 pontos)
risk: Low
```

---

## Descrição

Como **coordenador**, quero ver quem vinculou cada participante Zoom a um aluno e quando — via tooltip na tela de presença — para ter rastreabilidade sobre alterações e identificar erros de vinculação.

**Contexto técnico:** A tabela `zoom_participants` já existe com campos `student_id` (FK para aluno vinculado). Não há registro de quem fez a vinculação nem quando. A solução é: (1) criar tabela `zoom_link_audit` para logar cada vinculação; (2) gravar um registro audit a cada chamada de vinculação em `/presenca/index.html`; (3) exibir tooltip "Vinculado por X em DD/MM" no badge de vínculo.

---

## Acceptance Criteria

- [x] **AC1 — Tabela de audit:** migration cria `zoom_link_audit(id, participant_id, student_id, action, performed_by, performed_at)`
- [x] **AC2 — Registro automático:** a cada vinculação ou desvinculação em `/presenca`, insere registro em `zoom_link_audit` com `action = 'linked' | 'unlinked'`
- [x] **AC3 — `performed_by`:** registra o identificador do operador — usa `localStorage` de sessão admin se disponível, senão `'admin'`
- [x] **AC4 — Tooltip em presença:** ao hover no badge "Vinculado" de um participante, exibe tooltip: "Vinculado em DD/MM/AAAA" (sem necessidade de mostrar quem, por simplicidade)
- [x] **AC5 — Sem impacto na performance:** audit insert é fire-and-forget (não bloqueia o fluxo principal de vinculação)

---

## Tasks

- [x] Criar migration `zoom_link_audit` — tabela com participant_id, student_id, action, performed_by (text), performed_at (timestamptz)
- [x] Em `/presenca/index.html`: após sucesso de vinculação (`UPDATE zoom_participants SET student_id = ...`), fazer INSERT em `zoom_link_audit` de forma assíncrona (sem await bloqueante)
- [x] Carregar último registro de audit por participant_id ao renderizar lista de vinculados
- [x] Exibir tooltip no badge "Vinculado" com data formatada em pt-BR
- [x] Testar: vincular participante → verificar registro criado em `zoom_link_audit` via Supabase Table Editor

---

## File List

- `supabase/migrations/YYYYMMDD_zoom_link_audit.sql` — nova tabela
- `presenca/index.html` — INSERT audit após vinculação + tooltip no badge

---

## Dependencies

- Nenhuma dependência de outras stories

---

## Definition of Done

- [x] Migration cria tabela com índice em `participant_id`
- [x] Cada vinculação gera exatamente 1 registro em `zoom_link_audit`
- [x] Tooltip aparece ao hover no badge "Vinculado" com data correta
- [x] Vinculação não fica mais lenta (audit é non-blocking)

---

## CodeRabbit Integration

```yaml
coderabbit:
  focus_areas:
    - "Confirmar que audit insert é fire-and-forget (sem await bloqueante)"
    - "Verificar que migration tem índice em participant_id"
    - "Checar que tooltip não aparece em participantes não vinculados"
  severity_threshold: low
```
