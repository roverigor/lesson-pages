# Story 15.3 — UI Alunos por Turma + Bulk Import CSV

**Epic:** EPIC-015
**Status:** Ready
**Type:** Frontend (cs-portal)
**Wave:** 4
**Estimated:** 12h (M)
**Primary Agent:** @dev

---

## O que fazer

Implementar tab "Alunos" em `/cs/students` com gestão CRUD por aluno (manual): listar todos alunos do sistema, drawer detalhado por aluno (com timeline futura 15.7), add/remover de cohorts, bulk import CSV (nome, phone, email + cohort_id opcional), audit log de mudanças.

## Business Value

- **FR-3 implementação:** CS gerencia alunos autonomamente
- **CSV import** acelera onboarding em massa (cenários high-ticket com lista pré-existente)
- **EC-19 audit:** mudança de phone preserva histórico
- **CS sem escopo restrito** (decisão user 2026-05-04 — vê TODOS alunos)

## Acceptance Criteria

- [ ] **AC-1:** `cs-portal/src/pages/students.js` impl completa
- [ ] **AC-2:** Tabela alunos: name, phone, email, cohorts (badges múltiplas), status active
- [ ] **AC-3:** Search debounced (name + phone + email)
- [ ] **AC-4:** Filtros: active / inactive / all + cohort selector
- [ ] **AC-5:** Drawer aluno: detalhes editáveis (name, phone, email)
- [ ] **AC-6:** Add aluno a cohort: multi-select cohorts disponíveis → INSERT student_cohorts
- [ ] **AC-7:** Remove aluno de cohort: UPDATE student_cohorts.active=false (soft)
- [ ] **AC-8:** Botão "Novo Aluno": modal manual create (name + phone + email + cohorts)
- [ ] **AC-9:** Audit log entries: ao mudar phone/email/cohort registra row em `student_audit_log` (tabela criada nesta story OU em 15.A?)
- [ ] **AC-10:** Botão "Importar CSV": upload file + preview + confirma
- [ ] **AC-11:** CSV parser: aceita colunas `name,phone,email,cohort_id?` em qualquer ordem (headers)
- [ ] **AC-12:** Dedup CSV: detecta phones duplicados na própria lista + relatório
- [ ] **AC-13:** Phone normalize: regex BR (+55, espaços, parênteses, hífens removidos)
- [ ] **AC-14:** Validação email format (regex simples)
- [ ] **AC-15:** Import resultado: X criados, Y atualizados (existing email/phone), Z erros (linha + motivo)
- [ ] **AC-16:** AC-21 spec passa
- [ ] **AC-17:** EC-19 implementado: AC envia phone novo → UPDATE + audit entry
- [ ] **AC-18:** EC-23 implementado: CSV duplicates → dedup + relatório

## Tasks/Subtasks

### T1 — List + Search + Filters (2h)

- [ ] T1.1 (60min) Page skeleton + tabela + filtros UI
- [ ] T1.2 (45min) Fetch students com cohorts join
- [ ] T1.3 (15min) Search debounced

### T2 — Drawer Detalhes + Edit (3h)

- [ ] T2.1 (60min) Drawer component reutilizável (slide-in from right)
- [ ] T2.2 (60min) Form edit aluno (name/phone/email)
- [ ] T2.3 (60min) Multi-select cohorts (add/remove)

### T3 — Audit Log (1.5h)

- [ ] T3.1 (60min) Migration adicional: tabela `student_audit_log` (ou story 15.A absorve)
- [ ] T3.2 (30min) Insert audit entry em UPDATE (changed_field, old_value, new_value, changed_by)

### T4 — CSV Import (4h)

- [ ] T4.1 (90min) Upload component + parser PapaParse-like (custom simples)
- [ ] T4.2 (60min) Preview tabela com validações (✓ ou ✗ por row)
- [ ] T4.3 (60min) Confirm import: bulk upsert + dedup + audit
- [ ] T4.4 (30min) Resultado relatório

### T5 — Phone Validation (30min)

- [ ] T5.1 (15min) Função `normalizePhone()` (regex BR)
- [ ] T5.2 (15min) Test units com casos comuns (+55 11 99999-9999, etc.)

### T6 — Testing (1h)

- [ ] T6.1 (30min) Test CRUD manual
- [ ] T6.2 (30min) Test CSV import (10 rows, 2 dups, 1 inválido)

## Dev Notes

### CSV Parser Simples

```javascript
function parseCSV(text) {
  const lines = text.trim().split(/\r?\n/);
  const headers = lines[0].split(',').map(h => h.trim().toLowerCase());
  return lines.slice(1).map((line, idx) => {
    const cols = line.split(',').map(c => c.trim());
    const row = {};
    headers.forEach((h, i) => row[h] = cols[i] || '');
    row._line = idx + 2; // +2 (1 header + 1 indexbase)
    return row;
  });
}

function normalizePhone(phone) {
  const digits = phone.replace(/\D/g, '');
  if (digits.length === 11 && !digits.startsWith('55')) return '55' + digits;
  if (digits.length === 13 && digits.startsWith('55')) return digits;
  return digits; // retorna como veio se não match
}
```

### Audit Log Schema (proposta)

```sql
-- Adicionar em 15.A migration OU criar aqui
CREATE TABLE IF NOT EXISTS student_audit_log (
  id          UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  student_id  UUID REFERENCES students(id) ON DELETE CASCADE,
  changed_field TEXT NOT NULL,
  old_value   TEXT,
  new_value   TEXT,
  changed_by  TEXT,
  changed_at  TIMESTAMPTZ DEFAULT now()
);
CREATE INDEX idx_audit_student ON student_audit_log(student_id, changed_at DESC);
```

### Refs

| Tópico | Local |
|---|---|
| FR-3 alunos CRUD | requirements.json |
| EC-19 phone audit | edge cases |
| EC-23 CSV dedup | edge cases |
| AC-21 spec CRUD | spec.md#5 CRUD CS |

### Constraints

- ❌ NÃO HARD DELETE alunos (soft via active=false)
- ❌ NÃO sobrescrever phone sem audit log
- ✅ Phone normalize antes save
- ✅ CSV size limit 10MB (V1)

## Testing Strategy

| Gate | Validação |
|---|---|
| List + filter | tabela renderiza com cohorts badges |
| CRUD manual | criar/editar/remover funcionam |
| CSV 10 rows | 9 OK + 1 erro com mensagem |
| Phone normalize | "+55 11 99999-9999" → "5511999999999" |
| Audit log | UPDATE phone insere row em audit |

## File List

| Arquivo | Ação |
|---|---|
| `cs-portal/src/pages/students.js` | **NEW** |
| `cs-portal/src/shared/csv-parser.js` | **NEW** |
| `cs-portal/src/shared/phone-normalize.js` | **NEW** |
| `cs-portal/src/shared/drawer.js` | **NEW** (reusable component) |
| `supabase/migrations/20260506100000_student_audit_log.sql` | **NEW** (ou absorver em 15.A) |

## Compliance — Article IV

- [ ] Schema students existe; só extends com audit log
- [ ] EC-19 user-decision documentado
- [ ] CSV pattern padrão

## Dependencies

| Bloqueada Por | Razão |
|---|---|
| 15.2 | Cohorts CRUD para selector |
| 15.A | Schema students + audit log |

| Bloqueia | Razão |
|---|---|
| 15.4 | Dispatch avulso usa search alunos |
| 15.7 | Drill-down timeline aluno |

## Risks

| ID | Risco | Severidade | Mitigação |
|---|---|---|---|
| 15.3-R1 | CSV malformado quebra parser | Medium | Try/catch + linha por linha relatório |
| 15.3-R2 | Phone normalize regex falha BR variants | Medium | Test cases + fallback "como veio" |
| 15.3-R3 | Bulk import 10k rows trava UI | Low | Volume baixo; chunked upsert if needed |

## Definition of Done

- [ ] 18 ACs completed
- [ ] CSV import test 50+ rows OK
- [ ] Audit log funcional
- [ ] Branch ready

## Change Log

| Data | Versão | Autor |
|---|---|---|
| 2026-05-05 | 1.0 | @sm (River) |
| 2026-05-05 | 1.1 | Validation GO batch (10/10) — Status Draft → Ready | @po (Pax) |
