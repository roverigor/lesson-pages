# Story 14.4 — Import batch de transcrições históricas com resumo AI

## Metadata

```yaml
story_id: 14.4
epic_id: EPIC-014
title: Import batch de transcrições Zoom históricas e geração de resumo AI
status: Ready
executor: "@data-engineer"
quality_gate: "@qa"
created_by: "@sm (River)"
created_at: 2026-04-16
complexity: M (5 pontos)
risk: Medium
```

---

## Descrição

Como **coordenador**, quero poder importar transcrições de gravações Zoom que já existem (anteriores ao webhook) e gerar resumos AI para elas — para que todas as aulas tenham resumo disponível no painel, não apenas as futuras.

### Contexto Técnico

- O webhook `recording.completed` já baixa VTT e gera resumo via GPT-4o-mini
- Funciona apenas para gravações NOVAS (forward-looking)
- Gravações históricas em `class_recordings` que já têm `video_url` mas não têm `transcript_text` nem `summary`
- A Zoom Cloud Recording API permite baixar transcrições de gravações existentes usando o download_url + access_token
- O token Server-to-Server OAuth já está configurado

---

## Acceptance Criteria

- [ ] **AC1 — Action batch_import_transcripts:** Nova action em `zoom-attendance` ou `zoom-webhook` que processa gravações sem transcrição
- [ ] **AC2 — Busca em class_recordings:** Seleciona registros onde `transcript_text IS NULL` e `video_url IS NOT NULL`
- [ ] **AC3 — Download VTT da Zoom API:** Para cada gravação, busca a transcrição via Zoom API (GET /meetings/{meetingId}/recordings)
- [ ] **AC4 — Geração de resumo AI:** Reutiliza `generateAndSaveSummary()` existente para gerar resumo a partir da transcrição
- [ ] **AC5 — Progresso incremental:** Processa em batches de 5 gravações por execução para respeitar rate limits da Zoom API
- [ ] **AC6 — Botão no admin:** Botão "Importar Transcrições Pendentes" na tab Gravações que chama a action
- [ ] **AC7 — Log de resultado:** Retorna contagem de transcrições importadas, resumos gerados, erros

---

## Scope

### IN
- Nova action `batch_import_transcripts` na edge function
- Query para encontrar gravações sem transcrição
- Chamada à Zoom API para baixar VTT
- Reutilização de `generateAndSaveSummary()`
- Botão no admin para trigger manual

### OUT
- Agendamento em cron (manual first, cron futuro se necessário)
- Re-processamento de transcrições que já existem
- Alterações no schema de `class_recordings`

---

## Tasks

- [ ] Ler `generateAndSaveSummary()` e mapear dependências
- [ ] Ler Zoom API docs para download de recording transcripts
- [ ] Criar action `batch_import_transcripts` na edge function
- [ ] Implementar query: `class_recordings WHERE transcript_text IS NULL AND video_url IS NOT NULL`
- [ ] Implementar download de VTT via Zoom API com batch de 5
- [ ] Reutilizar `generateAndSaveSummary()` para cada transcrição
- [ ] Adicionar botão na UI admin (tab Gravações)
- [ ] Testar: importa transcrição de gravação existente
- [ ] Testar: gera resumo AI corretamente
- [ ] Testar: re-execução não reprocessa gravações já com transcrição

---

## Dependencies

- `zoom-webhook/index.ts` — função `generateAndSaveSummary()` existente
- Tabela `class_recordings` — schema existente
- Zoom Server-to-Server OAuth — token já configurado
- OpenAI API key — já configurada em env vars
